# import abc

# from query_builder_lib.query_builder_interface import QueryBuilderInterface

# class QueryBuilder(QueryBuilderInterface):
class QueryBuilder:
	def sendQuery(self, msg: str) -> str:
		#ToDo: Figure out what the "msg" is going to be, what kind of query it's going to send back.
		return msg
